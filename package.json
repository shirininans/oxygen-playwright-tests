{
  "name": "earth-oxygen-tests",
  "version": "1.0.0",
  "scripts": {
    "test": "npx playwright test",
    "codegen": "npx playwright codegen"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}